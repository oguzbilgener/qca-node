name: limit
in: query
description: |
  Maximum number of objects to return. If not provided, the default limit is 25.
  The server will return at most 1000 objects at once.
schema:
  type: integer
  default: 25
  minimum: 1
  maximum: 1000
  example: 25
